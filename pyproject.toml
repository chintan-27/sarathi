[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "sarathi"
version = "0.1.0"
description = "AI-powered results-to-presentation tool"
requires-python = ">=3.10"
dependencies = [
    "click>=8.0",
    "watchdog>=3.0",
    "ollama>=0.3",
    "playwright>=1.40",
    "Pillow>=10.0",
    "rich>=13.0",
    "python-dotenv>=1.0",
    "pandas>=2.0",
    "matplotlib>=3.8",
    "seaborn>=0.13",
    "python-pptx>=0.6",
    "flask>=3.0",
    "anthropic>=0.40",
    "psutil>=5.9",
]

[project.scripts]
sarathi = "sarathi.cli:cli"

[tool.hatch.build.targets.wheel]
packages = ["src/sarathi"]
